import 'package:flutter/material.dart';

class ComidaContent extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text(
        'Disfruta de comidas tradicionales y opciones internacionales para todos los gustos.',
        style: const TextStyle(fontSize: 20),
        textAlign: TextAlign.center,
      ),
    );
  }
}
